# ============================================================
# DZ PRIME ACADEMY — Environment Variables
# ============================================================
# PUBLIC (safe in the browser) — already provisioned in .env
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# SERVER ONLY (never exposed to the browser)
# Managed through Lovable secrets, NOT committed to git.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
TELEGRAM_BOT_TOKEN=
